# ドメイン移行の手順（genre-roots.com）

2026-09-01 決定。`chororinkob.github.io/genre-roots/` から
`genre-roots.com` へ移す。

**いきなり切り替えない。** 順番を間違えると、いま見に来ている人から
サイトが消える。下の順でやる。

---

## ⚠ いちばん大事な注意

**`CNAME` ファイルをリポジトリに置くのは、DNSの設定が終わってから。**

先に置くと、GitHub Pages は `chororinkob.github.io/genre-roots/` へのアクセスを
`genre-roots.com` へ転送し始める。ドメインがまだ向いていないと、
**転送先が存在せず、サイトが誰からも見られなくなる。**
いま友達が見ている状態なので、これは避ける。

---

## 手順

### 1. ドメインを買う（チョロさん）

**業者：Spaceship（spaceship.com）／1年分**（2026-09-01 決定）

7社を5年総額で比べて決めた。

| 業者 | 初年度 | 更新 | 5年合計 |
|---|---|---|---|
| **Spaceship** | $4.99 | **$9.98** | **$44.91** |
| Cloudflare | $10.44 | $10.44 | $52.20 |
| Dynadot | $10.88 | $10.88 | $54.40 |
| Porkbun / NameSilo | $10.99 | $10.99 | $54.95 |
| Namecheap | $5.98 | $13.98 | $61.90 |
| GoDaddy | $0.99 | $22.99 | $92.95 |

**値段には床がある。** .com の卸値は Verisign 1社が決めていて、いまは
$10.26 ＋ ICANN手数料 $0.18 = **$10.44**。これより安く売る業者は赤字なので、
「びっくりするほど安い」は原理的にありえない。GoDaddy の初年度99セントは
更新で回収する形（日本の お名前.com も同じ作り）。

**Spaceship の更新 $9.98 は卸値より安い＝赤字の客寄せ。** いつ上げても
おかしくない。上がったら Cloudflare（原価売りを方針に掲げている）へ
移せばよい。移管は1年分の支払いで、その1年が期限に足されるので損はしない。

**買うときに必ずやること**
- 購入画面で `genre-roots.com` が本当に空いているか確かめる
- **自動更新を入れる。** 1年分なので、切らすと他人に取られる
- WHOIS非公開（無料）を有効にする
- 更新日を控えておく

**2026年11月1日に卸値が $10.97 へ上がる**（+6.9%）。契約上あと3回、
毎年最大7%まで上げられ、$13.42 前後まで行きうる。
1年分にしたので、来年の更新はこの値上げ後の値段になる。

### 1-2. Spaceship の画面での流れ（買うところまで）

1. **spaceship.com** を開く
2. 右上の **Sign up** で登録する（メールアドレスとパスワードだけ）。
   確認のメールが届くので、中のリンクを押して有効にする
3. 検索欄に **genre-roots.com** と入れて探す
4. 出てきたら **買い物かごに入れる**
5. 期間を **1 year** にする
6. 支払い画面で、**余計なものが付いていないか確かめる**。
   ホスティング・メール・SSL などの勧めが混ざっていたら外す。
   **WHOIS非公開（privacy）は無料なので付けたまま**にする
7. 支払う（クレジットカード。表示はドル建て）
8. 買えたら、その場で **自動更新（Auto-renew）を入れる**。
   1年分なので、切らすと他人に取られる。ここだけは必ず

---

### 2. DNS を設定する（チョロさん・Spaceship の画面）

**画面の道順**

1. ログインした状態で、画面の上のほうにある **虫めがねのしるし（🔍）** を押す
2. **Advanced DNS** と打って、出てきたものを開く
3. 一覧から **genre-roots.com** を選ぶ

**先に、もともと入っている記録を消す**

買った直後は「準備中のページ」へ向ける記録が最初から入っていることが多い。
残っていると、こちらの設定と喧嘩して**どちらが効くか分からなくなる**。
A や CNAME の記録が既にあれば、先に消しておく。

**入れる記録**

`DNS records` の中の `Custom records` から、`+ Add record` で足していく。

| 種類 | Host（名前） | Value（値） |
|---|---|---|
| A | `@` | `185.199.108.153` |
| A | `@` | `185.199.109.153` |
| A | `@` | `185.199.110.153` |
| A | `@` | `185.199.111.153` |
| CNAME | `www` | `chororinkob.github.io` |

- **`@` は「ドメインそのもの」という意味**。画面によっては空欄のままにする作りの
  ところもある。入れたあと一覧に `genre-roots.com` と出ていれば合っている
- Host に `genre-roots.com` と丸ごと打たないこと。二重になって
  `genre-roots.com.genre-roots.com` になってしまう
- TTL（有効期間）は既定のままでよい

IPv6（AAAA）も入れられるなら、Host は同じく `@` で下の4つを足す。無くても動く。

```
2606:50c0:8000::153
2606:50c0:8001::153
2606:50c0:8002::153
2606:50c0:8003::153
```

**入れ終わったらクローディアに知らせる。** 世界のDNSに広まったかを
こちらで確かめる（数分〜1時間ほどかかる）。効いていることを確認してから
次へ進む。


**A レコード**（`genre-roots.com` そのもの）を4つ。実測で確かめた値。

```
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```

**AAAA レコード**（IPv6）も4つ入れておくとよい。

```
2606:50c0:8000::153
2606:50c0:8001::153
2606:50c0:8002::153
2606:50c0:8003::153
```

**CNAME レコード**（`www` から本家へ）

```
www  →  chororinkob.github.io
```

Spaceship の DNS をそのまま使えばよい。よそのDNSに移す必要はない。

※ もし将来 Cloudflare へ移した場合は、**プロキシを切る（グレーの雲にする）**こと。
オレンジのままだと GitHub が証明書を出せず、鍵マークが付かない。

### 3. サーバー（Render）を先に出す（クローディア）

`server.py` の「受け付ける相手」に `genre-roots.com` を足してある（2026-09-01）。
**これを先に公開しておかないと**、新しいドメインから
マイクの判定・ジャンル追加・修正依頼・記録が全部はねられる。

※ Render を出すとアクセスの記録が消える。「記録を消えない場所に置く」
作業を先に済ませておけば、失わずに済む。

### 4. CNAME ファイルを置く（クローディア）

DNS が効いているのを確かめてから、リポジトリの一番上に `CNAME` という
名前のファイルを作り、中身を1行だけ書く。

```
genre-roots.com
```

置いた時点で `chororinkob.github.io/genre-roots/` は新しいドメインへ
自動で転送されるようになる（前のURLを開いている人も飛ばされる）。

### 5. 鍵マーク（HTTPS）が付くのを待つ

GitHub が自動で証明書を用意する。数分〜1時間ほど。
リポジトリの設定画面で「Enforce HTTPS」に印を付ける。

### 6. 埋め込まれているURLを直す（クローディア）

`chororinkob.github.io` と書いてある場所が **約30か所**ある。
とくに大事なのは、共有したときに出る画像と説明（`og:url` `og:image`）。
ここが古いままだと、SNSに貼ったときに前のURLが出る。

| ファイル | 数 |
|---|---|
| `genre_roots.html` | 5 |
| `docs/about.html` | 5 |
| `index.html` | 4 |
| `docs/manual.html` | 4 |
| `music_genre_finder/templates/contact.html` | 3 |
| `music_genre_finder/templates/admin_pending_list.html` | 3 |
| その他（道具・仕様書など） | 数か所 |

**置き場所が変わることに注意。** いまは `/genre-roots/` という階層の下に
あるが、独自ドメインでは一番上に来る。

```
前： chororinkob.github.io/genre-roots/genre_roots.html
後： genre-roots.com/genre_roots.html
```

### 7. 確かめる（クローディア）

- 新しいURLで地図・ガイド・「このサイトについて」が開くか
- 鍵マークが付いているか
- マイク・ジャンル追加・修正依頼が動くか（CORSが通っているか）
- 共有したときの画像と説明が新しいURLになっているか
- 前のURLから転送されるか

### 8. 検索のことは急がない

Google に新しいURLを覚え直してもらうには数週間かかる。
転送さえ効いていれば評価は引き継がれるので、待てばよい。

---

## いまどこまで済んでいるか

- [x] ドメイン名を決めた（genre-roots.com・2026-09-01）
- [x] 買う業者を決めた（Spaceship・1年分・7社を5年総額で比較して決定）
- [x] 空いていそうなことを確かめた（DNSで確認。正式な確認は購入画面で）
- [x] GitHub Pages のIPを実測で確かめた
- [x] `server.py` の受け付ける相手に足した（**公開はまだ**）
- [ ] ドメインを買う（自動更新を入れるのを忘れない）
- [ ] DNS を設定する
- [ ] サーバーを出す
- [ ] CNAME を置く
- [ ] URLを直す
- [ ] 確かめる
