# 図の宿題

使い方ガイド（`docs/manual.html`）の図について、気づいたことを貯めておく場所。

**画面を直すたびに撮り直さない。**ここに書き足しておいて、手直しが落ち着いた
ところで、まとめて撮り直す。

```
node tools/figs/all.js
```

これで36枚がぜんぶ、そのときの画面に入れ替わる。使い方は `tools/figs/README.md`。

**流す前に、直した内容を公開しておくこと。**手元のファイルから撮ると
YouTube が動画を貸してくれず、図にエラーが写り込む。

---

## チョロさんとクローディアの約束ごと

### 宿題に足すとき

**決まった言い方は要らない。**図の話が出たら、クローディアが自分でここに
書き足す。「あとで直すね」で終わらせず、必ずこのファイルに残すこと。

チョロさんから念を押したいときは、こんな言い方でよい。

> 「これ、図の宿題に入れといて」

### 撮り直すとき

**クローディアが勝手に流さない。**チョロさんが「いいよ」と言ってから。

理由は3つ。
 ・撮る前に「直した内容を公開しておく」段取りが要る
 ・36枚が一度に差し替わるので、変更が大きくなる
 ・20分ほどかかる

チョロさんからの合図は、こんな言い方でよい。

> 「図を撮り直して」／「一段落したから、図を更新して」

### いつ言えばいいか

**クローディアから声をかける。**次のときに「そろそろ撮り直しませんか」と
提案すること。チョロさんが覚えておく必要はない。

 ・宿題が3件くらい溜まったとき
 ・ヘッダーや説明パネルなど、**多くの図に写る場所**をいじったとき
 ・ジャンル数が節目を越えたとき（画面の数字が図と食い違うため）
 ・公開の直前

### 自動で定期的に流すのはやめておく

やろうと思えばできる（GitHub Actions で毎週など）。ただし、
 ・YouTube の広告が写ることがあり、**人の目で見ないと気づけない**
 ・思わぬ画面（読み込み中、通信の失敗）のまま撮れてしまうことがある
 ・36枚が黙って差し替わり、誰も見ていない変更が積み上がる
ため、**撮ったら必ず目で確かめる**という前提を崩さない。

---

## いま残っていること

（いま残っている宿題はありません。気づいたら、日付と「どの図が・何がおかしいか」をここに足していく。）

---

## 注意（次に `node tools/figs/all.js make_figs` のように一部だけ流すとき）

2026-09-05に実際に起きた事故。`fig01`の文言だけ直したくて
`node tools/figs/all.js make_figs` を単独で流したところ、
本来は `make_figs.js` の後に `fig4_redo.js` が上書きするはずだった
`fig04_gosenfu` が、`make_figs.js` の古い（実際にはクリックしていない、
CSSクラスを直接付け替えて偽の光り方をさせただけの）版に巻き戻ってしまった。
**`一部だけ` 指定で流すときは、その図を最後に上書きする redo 系のスクリプトが
無いか `all.js` の手順一覧を確認し、あれば一緒に指定すること**
（例: `node tools/figs/all.js make_figs fig4_redo fig7_redo`）。

## 片づいたこと

### 2026-09-05（2回目）── fig04の矢印の分かりやすさ・章構成・画像の大きさを再修正

1回目の直しを見たチョロさんから再度の指摘。

- **fig04_gosenfu の矢印がめちゃくちゃ**：3つの矢印がそれぞれ別の、
  ラベルの無い密集地帯を指していて、どれがどの道の説明か分からなかった。
  ラベルの見える1本の道（Jazz→Bebop）だけを選び、その道の3か所を
  順になぞる形に直した（`fig4_redo.js`）
- **ト音記号の説明が回りくどい**：「飾りで意味はなし」をやめ、
  「ト音記号は『五線譜』という意味。強弱記号は線の本数と同じ5段階」に簡略化
- **マップの説明に「代表曲をクリックする」が紛れ込んでいた**：
  パネル側の話なので削除。「1.丸をクリック」「2.関連リンクをたどる」の2手順に
- **「説明パネルの見方」の章に「色の見本をクリック」の内容（実は
  マップの話）が紛れ込み、章のリンク先がずれていた**：本来の第1章
  （マップの見方・丸の色のすぐ後）に移動し、第2章はパネルの中身だけにした
- **パネル画像が実物の2倍以上の大きさで表示されていた**：実物のパネル幅
  （300〜400px程度）に対して700px幅で表示していたのが原因。`.fig-panel`
  というCSSクラス（最大幅320px）を新設して縮小
- **Spotifyの説明文が分かりにくい・一覧が写っていない画像は不要**：
  「一覧のすぐ下にあります。押すとSpotifyで検索結果が別タブで開きます」に
  整理し、外部画像は削除
- **「音楽性・特徴」の説明が無いままいきなり用語ふきだしの話になっていた**：
  本文だけの図（`pnl2a_desc`）とふきだしの図（`pnl2b_gloss`）を分けて撮り直し、
  本文の説明→ふきだしの説明の順にした
- **操作説明にAmazonアフィリエイトの言及は不要**：該当文を削除
  （about.htmlのみに残す）

### 2026-09-05 ── 説明パネルの見方を大幅加筆、五線譜の道を撮り直し

チョロさんがヘルプを最初から最後まで読んで見つけた指摘に対応。

- **fig04_gosenfu**：上の「注意」のとおり、実際にはクリックしないと
  起きない「パネルが無いのに影響のラインだけ出ている」状態になっていた。
  `fig4_redo.js`を直し、実際にジャンルをクリックした本物のパネルが
  開いた状態で撮り直した。あわせて「音符が流れる向き＝影響の向き」
  「ト音記号は飾りで意味なし、強弱記号pp〜ffは線の本数と同じ5段階」を
  実測データから注釈するようにした
- **PC版「説明パネルの見方」がほぼ空だった**：「丸をクリックすると、
  右のパネルの見方です」の1文で終わっていた。①ジャンル名②知名度
  ③別名・発祥時期④代表曲の動画⑤Spotify「もっと聴く」⑥音楽性・特徴
  ⑦用語のふきだし⑧成り立ち・関連する書籍⑨影響の度合い、修正依頼
  ボタンまで、新規の図7枚（`pnl1_top`〜`pnl7_amazon`、うち2枚は
  Spotify・Amazonへ実際に飛んで撮った外部サイトの画面）を添えて
  全面的に書き直した（`tools/figs/panel_detail_figs.js`を新設）

### 2026-09-04 ── 36枚をまとめて撮り直した（ヘルプの抜本的見直しの一環）

- **ボタンの名前の変更**（「これなんて曲？」→「この曲なんてジャンル？」）が
  4枚（`fig01_gamen` `pcmic1_open` `sp03_tab_after` `spmic1_open`）に反映された
- **ヘッダーの著作権表示（© 2026 チョロ）が消えたこと**が全図に反映された
  （2026-09-04にヘッダーから削除・名義もChororinkobへ統一したため）
- **版・ジャンル数**が最新（v28.3・573ジャンル・2026/9/4）に更新された
- 撮影中に**新しい不具合を発見して修正**：`fig01_gamen`の吹き出しに
  「ヘルプ・全体表示・このサイトについて」という固定文言が残っていたが、
  「このサイトについて」ボタンは今回ヘッダーから削除済みで実際の画面と
  食い違っていた。`tools/figs/make_figs.js`の文言を「ヘルプ・全体表示」に
  直してから撮り直した
- 4枚（fig01・sp03・pcmic1・sp13・kakunin1）を目視で確認し、
  ヘッダー・ボタン名・版表示が実際の画面と一致することを確認済み

### サーバー側の名前の直しは、すでに公開済みだった（2026-09-04に確認）

このメモには「pushするとRenderの記録が消えるので出していない」とあったが、
2026-09-02にguard.pyの記録を外部の保管庫（genre-roots-logsリポジトリ）へ
送る設定を有効にしたことで、この制約はすでに解消されていた
（→ プロジェクトメモリ「いたずら対策の実装状況」参照）。
実際に本番の地図を確認したところ、ボタン名はすでに新しい名前で
表示されていることを確認できた。

### 2026-09-02 ── 用語集の「ドロップC」「ドロップD」を直した

Death Metal・Stoner Rock・Nu Metal の説明にある「ドロップC」「ドロップD」
（ギターの調弦名）に、EDMの「ドロップ（音圧が増す瞬間）」の説明が出ている。
言葉は同じでも意味がまったく違う。用語集に「ドロップC」「ドロップD」を
別の語として足せば直る（長い語から先に当てる作りなので、足すだけでよい）。

※ 単語の途中に説明が割り込む不具合（TSUNAMI→TSUNAM［AM の説明］I など
528か所）は 2026-08-31 に v27.38 で直した。これは残った別種の4か所。

用語集に2語を足して解決。長い語から先に当てる作りなので、足すだけで
正しい説明（ギターの調弦）が出るようになった。
Death Metal・Stoner Rock・Nu Metal を実際に開いて確認済み（○9/✗0）。

### 2026-08-31 ── 36枚をまとめて撮り直した

- **もう無いボタンが写っていた**（7枚）
  `fig01` `fig02` `fig03` `fig05` `fig06` `fig07` `pcmic4_map` に
  「配置を保存」「配置リセット」が写っていた。2026-08-30 にこの2つを
  取り除いたので、画面を探しても見つからないボタンを説明している状態だった。
- **古い版のまま**だった（v26.89・572ジャンル・2026年8月25日現在）
- **スマホのヘッダーが古い**（15枚）
  `sp01`〜`sp14` `spmic4_map`。高さ44px・著作権の表示なしのまま。
  いまは35pxで「© 2026 チョロ」が入る。

### 2026-08-30 ── 図4を撮り直した

- **動画が「エラー 153」**になっていた。手元のファイルを `file://` で開いて
  撮っていたため、YouTube が埋め込みを拒否していた。公開しているURLから
  撮るようにして解決。
- 撮り直しの途中で **YouTube の広告が写り込んだ**。通信を止めても防げない
  ので、動画の枠の中の「スキップ」を押してから撮るようにした。
- 矢印が **レベル2とレベル1** という比べようのない道を指していた。
  線の「まんなか」で選んでいたため、いちばん太い道が候補から落ちていた。

---

## 書き方

気づいたら、日付と「どの図が・何がおかしいか」を上の「いま残っていること」に
足していく。撮り直したら「片づいたこと」へ移す。
