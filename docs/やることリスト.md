# やることリスト

公開に向けて残っていることを、ひとつの場所にまとめておく。
セッションをまたぐと覚え書きの数字が古くなるので、**着手する前に必ず
実物を見て確かめる**こと（このファイル自体も古くなる）。

図の撮り直しについては `図の宿題.md` に分けてある。

最終更新：2026-09-01

---

## いまの版

| もの | 版 | どこ |
|---|---|---|
| 地図 | v27.54 | GitHub Pages（公開済み） |
| 使い方ガイド | v1.17 | 同上 |
| マイクパネル（地図に埋め込み） | v2.39 | 同上 |
| マイクパネル（サーバー側の元） | v2.39 | **未公開** |
| いたずら対策 guard.py | v1.03 | **未公開の変更あり** |
| ジャンル追加 genre_adder.py | v2.31 | 公開済み |

---

## A. すぐできる

### A-1. 用語集の「ドロップC」「ドロップD」

ギターの調弦名なのに、EDMの「音圧が増す瞬間」の説明が出ている。
用語集にこの2語を別の語として足せば直る（長い語から先に当てる作りなので、
足すだけでよい）。

該当：Death Metal・Stoner Rock・Nu Metal

### A-2. カテゴリ一覧の並び順を知名度順にそろえる

知名度の作業（v26.43）のときの積み残し。

---

## B. 合図をもらってから動く

### B-1. 図の撮り直し（36枚）

`図の宿題.md` を参照。20分ほどかかる。**クローディアが勝手に流さない。**

### B-2. サーバー側（Render）の公開

3つのファイルに直しが入っているが、**まだ公開していない**。
Render を入れ替えるとアクセスの記録が消えるため、次にサーバーを
触るときに一緒に出す。**利用者の画面には出ないものなので急がない。**

| ファイル | 何が入っているか |
|---|---|
| `templates/mic_panel.html` | ボタンの名前・動画の playsinline と sandbox（v2.39）|
| `templates/admin_stats.html` | 統計画面のカード名 |
| `guard.py` | 上限に引っかかったときの記録の文言（2か所）|

※ マイクパネルは地図に埋め込まれたものが画面に出る。サーバー側は元ファイルで、
2026-07-15（genre_adder v1.55）に注入をやめて以来、利用者には出ていない。

---

## C. 決めごとが要る

### C-1. 問い合わせ・修正依頼の通知が誰にも届かない

フォームは動いているが、知らせが来ない。メールは 2026-07-14 に全廃した。
Render は SMTP を塞ぐので、送るなら Resend が要る。
**公開したら、誰かが依頼を出しても気づけない。**

### C-2. 573ページのSEO

見た目はそのままで、中身を分割して検索に引っかかるようにする話。
「ジャンル名で検索した人に届く」という目的に直結する。

### C-3. アクセスの記録がサーバー入れ替えで消える（ずっと保留の「2番」）

記録専用の置き場を決める必要がある。

### C-4. 支援ボタン

Ko-fi の登録待ちで止まっている。

### C-5. 独自ドメイン

取るかどうか。維持費の確認から。

---

## 片づいたこと（2026-09-01）

- ボタンの名前を「この曲なんてジャンル？」に変更（パソコン・スマホとも2段）
- 共有画像の小さい字を売り文句に差し替え
- **スマホで動画を押すと画面ごと切り替わる**（playsinline が8か所抜けていた）
- **広告を踏むと事典から連れ去られる**（sandbox が無かった）
- **「YouTubeで見る」で別タブが開く**（allow-popups を外した）
- **代表曲を押しても止まったまま**（枠ごと作り直していた → 中身だけ差し替え）
- **一度鳴らすと他のジャンルでも勝手に鳴る**（パネルを開いたときの自動再生）
- 代表曲の重複 … 実データを数えたら残り1曲だけだった（覚え書きの「94件」は古い）
